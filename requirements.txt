# Core pipeline (parsing, normalization, rule checking, aggregation, plots)
pydantic>=2.0
pandas>=1.3
matplotlib>=3.5

# scripts/02_summarize_results.py only needs the above — no GPU required.
# scripts/01_run_pipeline.py additionally needs vLLM (see requirements-judge.txt).
