[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "faa-cxer-no-groundtruth"
version = "0.1.0"
description = "No-ground-truth ATC conversation error detection: rule-based + LLM hybrid pipeline"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
include = ["faa_cxer_ng*"]
